# --- Baseline (lokal, CPU) ---
scikit-learn>=1.4
pandas>=2.0
numpy>=1.26
scipy>=1.11
joblib>=1.3

# --- API + arayüz ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- BERTurk backend (BACKEND=berturk ile servis için lokalde gerekir) ---
# pip install torch transformers
# transformers>=4.40
# torch

# --- Faz 2: BERTurk fine-tune (yalnızca Colab'da) ---
# datasets
# accelerate
